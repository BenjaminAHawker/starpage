import { link } from "types";

interface linkObject {
    [x:string] : link[]
} 

const links:linkObject = {
    work:[
        {label:'',icon:'',href:''}
    ],
    home:[
        {label:'',icon:'',href:''}
    ]
};