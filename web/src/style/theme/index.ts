import {Colors} from '@styles';

const dark = {
    bg:`${Colors.MainColors.MainPalette.oxfordBlue}`,
};

const lite = {
    
};


const Theme = {
    dark:dark,
    lite:lite,
};

export default Theme;